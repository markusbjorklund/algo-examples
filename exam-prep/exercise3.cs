using System;
using System.Collections.Generic;

namespace examprep
{
  class Rooms
  {
    public int Calculate(int[, ] input)
    {
      
      return 0;
      // You are given a map of rooms in a house, and your task is to calculate, how many rooms a house has. A map is a grid where each square is either floor (marked with 0) or wall (marked with 1). Two floor squares are in the same room, if they are adjacent horizontally or vertically. You can assume each square on the outer perimeter is wal

    }
  }
}